import * as React from 'react';
import Login from './Login';

export interface INologinProps {
}

export default function Nologin (props: INologinProps) {
  return (
    <div>
    <Login></Login>
    </div>
  );
}
